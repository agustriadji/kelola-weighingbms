FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start development server with database setup
CMD ["sh", "-c", "sleep 10 && npm run db:setup && npm run dev"]