FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 3001

# Set Next.js to listen on all interfaces
ENV HOSTNAME=0.0.0.0

# Start development server
CMD ["sh", "-c", "sleep 10 && npm run dev -- -p 3001"]